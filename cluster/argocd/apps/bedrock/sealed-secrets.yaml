apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: sealed-secrets
spec:
  project: default
  source:
    repoURL: 'git@github.com:politeauthority/colfax-ops.git'
    path: cluster/bedrock/sealed-secrets
    targetRevision: main
    helm:
      valueFiles:
        - values-colfax.yaml
  destination:
    namespace: kube-system
    name: in-cluster
  syncPolicy:
    automated:
      selfHeal: true
    syncOptions:
      - ServerSideApply=true
